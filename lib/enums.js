const ROOT_TRACKER_ATTRIBUTE = 'data-tracker-root';
const TRACKING_ATTRIBUTE = 'data-tracker';

const TRACKING_TYPE = Object.freeze({
  SEEN: 'seen',
  CLICK: 'click',
});

module.exports = {
  TRACKING_TYPE,
  ROOT_TRACKER_ATTRIBUTE,
  TRACKING_ATTRIBUTE,
};
